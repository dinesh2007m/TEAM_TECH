import React from 'react';
import { PlaceholderPage } from './PlaceholderPage';

export const Login = () => {
  return (
    <PlaceholderPage
      title="Authentication Portal"
      subtitle="Enterprise SSO & Multi-Factor Security Login Gateway"
      iconName="Lock"
      badgeText="LOGIN ROUTE"
      description="Login route placeholder configured. Authentication logic and login page will be implemented in subsequent phases."
    />
  );
};
