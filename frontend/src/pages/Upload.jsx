import React from 'react';
import { PlaceholderPage } from './PlaceholderPage';

export const Upload = () => {
  return (
    <PlaceholderPage
      title="Upload Infrastructure Scan"
      subtitle="Ingest raw vulnerability logs, Nmap XML, and Cloud PCAP traces"
      iconName="UploadCloud"
      badgeText="UPLOAD ROUTE"
      description="Upload scan route placeholder created."
    />
  );
};
